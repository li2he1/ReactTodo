export default function TodoFT(props) {
  return (
    <div>
      <button onClick={props.showMore}>Show more</button>
      {/* <button>Show Done</button>
      <button>Show Not Done</button>
      <button>Show All</button> */}
    </div>
  );
}
